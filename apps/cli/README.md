# WorkCue CLI

WorkCue is a local-first morning brief CLI for work scattered across existing tools.

```bash
workcue today --demo
workcue init --output .workcue/config.yml
workcue doctor --config .workcue/config.yml
```

This package is part of the WorkCue monorepo.
